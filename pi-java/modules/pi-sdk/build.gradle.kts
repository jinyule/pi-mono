description = "Stable public facade for embedding pi-java in other JVM applications."

dependencies {
    api(project(":pi-ai"))
    api(project(":pi-agent-runtime"))
    api(project(":pi-session"))
}

